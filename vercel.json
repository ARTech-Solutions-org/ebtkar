{
  "installCommand": "npm install --ignore-scripts --no-audit --prefer-offline || npm install --no-audit",
  "buildCommand": "npx vite build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
